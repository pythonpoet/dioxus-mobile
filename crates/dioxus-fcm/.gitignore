/target

# Real Firebase config, copied here at build time by build.rs from either
# <app>/ios/GoogleService-Info.plist or GoogleService-Info.example.plist. Never
# commit the real file — it's per-app and per-Firebase-project.
/src/ios/plugin/Sources/Resources/GoogleService-Info.plist
